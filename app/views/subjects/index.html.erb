
<div class="container">
  <div class="row">
    <div class="col">
      <div class="subjectsContainer">
        <h4>Subject Categories</h4>
        <p><%= link_to 'New Subject', new_subject_path %></p>
        <div class="searchContainer">
          <%= form_tag(subjects_path, method: :get) do %>
            Enter a Keyword: <%= text_field_tag :keyword, params[:keyword], class:"searchBox", placeholder: "Subject Name" %>
            <div class="submitTag">
            <%= submit_tag "Search", class: "searchfilterButton" %>
            </div>
          <% end %>
        </div>
      </div>
      <div class="container">
        <div class="row">
          <div class="col">
            <table class="subjectsTable table table-responsive-md">
              <%= render partial: "subjectsTable", locals: {subjects: @subjects} %>
            </table>
          </div>
        </div>
      </div>
      <%#= will_paginate @subjects %>
    </div>
  </div>
</div>
